import React from 'react';
import { hot } from 'react-hot-loader/root';

function App() {
  return (
    <div id="app-container">
      <h1>Boilerplate App is working!</h1>
    </div>
  );
}

export default hot(App);
